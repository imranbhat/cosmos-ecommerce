apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
type: Opaque
stringData:
  # In a real production scenario, use sealed secrets or external vault.
  # For now, using Opaque secrets with stringData for readability in this generation.
  POSTGRES_PASSWORD: password
